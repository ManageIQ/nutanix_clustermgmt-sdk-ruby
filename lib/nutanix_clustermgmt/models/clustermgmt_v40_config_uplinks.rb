=begin
#Nutanix Cluster Management APIs

#Manage Hosts, Clusters and other Infrastructure.

The version of the OpenAPI document: 4.0.2

Generated by: https://openapi-generator.tech
Generator version: 7.19.0

=end

require 'date'
require 'time'

module NutanixClustermgmt
  # Active and standby uplink information of the target nodes.
  class ClustermgmtV40ConfigUplinks < ApiModelBase
    # Active uplink information.
    attr_accessor :active

    # Standby uplink information.
    attr_accessor :standby

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'active' => :'active',
        :'standby' => :'standby'
      }
    end

    # Returns attribute mapping this model knows about
    def self.acceptable_attribute_map
      attribute_map
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      acceptable_attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'active' => :'Array<ClustermgmtV40ConfigUplinksField>',
        :'standby' => :'Array<ClustermgmtV40ConfigUplinksField>'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `NutanixClustermgmt::ClustermgmtV40ConfigUplinks` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      acceptable_attribute_map = self.class.acceptable_attribute_map
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!acceptable_attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `NutanixClustermgmt::ClustermgmtV40ConfigUplinks`. Please check the name to make sure it's valid. List of attributes: " + acceptable_attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'active')
        if (value = attributes[:'active']).is_a?(Array)
          self.active = value
        end
      end

      if attributes.key?(:'standby')
        if (value = attributes[:'standby']).is_a?(Array)
          self.standby = value
        end
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      warn '[DEPRECATED] the `list_invalid_properties` method is obsolete'
      invalid_properties = Array.new
      if !@active.nil? && @active.length > 1024
        invalid_properties.push('invalid value for "active", number of items must be less than or equal to 1024.')
      end

      if !@active.nil? && @active.length < 0
        invalid_properties.push('invalid value for "active", number of items must be greater than or equal to 0.')
      end

      if !@standby.nil? && @standby.length > 1024
        invalid_properties.push('invalid value for "standby", number of items must be less than or equal to 1024.')
      end

      if !@standby.nil? && @standby.length < 0
        invalid_properties.push('invalid value for "standby", number of items must be greater than or equal to 0.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      warn '[DEPRECATED] the `valid?` method is obsolete'
      return false if !@active.nil? && @active.length > 1024
      return false if !@active.nil? && @active.length < 0
      return false if !@standby.nil? && @standby.length > 1024
      return false if !@standby.nil? && @standby.length < 0
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] active Value to be assigned
    def active=(active)
      if active.nil?
        fail ArgumentError, 'active cannot be nil'
      end

      if active.length > 1024
        fail ArgumentError, 'invalid value for "active", number of items must be less than or equal to 1024.'
      end

      if active.length < 0
        fail ArgumentError, 'invalid value for "active", number of items must be greater than or equal to 0.'
      end

      @active = active
    end

    # Custom attribute writer method with validation
    # @param [Object] standby Value to be assigned
    def standby=(standby)
      if standby.nil?
        fail ArgumentError, 'standby cannot be nil'
      end

      if standby.length > 1024
        fail ArgumentError, 'invalid value for "standby", number of items must be less than or equal to 1024.'
      end

      if standby.length < 0
        fail ArgumentError, 'invalid value for "standby", number of items must be greater than or equal to 0.'
      end

      @standby = standby
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          active == o.active &&
          standby == o.standby
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [active, standby].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      transformed_hash = {}
      openapi_types.each_pair do |key, type|
        if attributes.key?(attribute_map[key]) && attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = nil
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[attribute_map[key]].is_a?(Array)
            transformed_hash["#{key}"] = attributes[attribute_map[key]].map { |v| _deserialize($1, v) }
          end
        elsif !attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = _deserialize(type, attributes[attribute_map[key]])
        end
      end
      new(transformed_hash)
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

  end

end
